<odoo>
    <record model="ir.ui.view" id="beesdoo_account_invoice_form_view">
        <field name="name">beesdoo.account.invoice.form.view</field>
        <field name="model">account.invoice</field>
        <field name="inherit_id" ref="account.invoice_form"/>
        <field name="arch" type="xml">
            <field name="date_invoice" position="attributes">
                <attribute name="required">True</attribute>
            </field>
        </field>
    </record>

    <record model="ir.ui.view" id="beesdoo_account_invoice_supplier_form_view">
        <field name="name">beesdoo.account.invoice.supplier.form.view</field>
        <field name="model">account.invoice</field>
        <field name="inherit_id" ref="account.invoice_supplier_form"/>
        <field name="arch" type="xml">
            <field name="date_invoice" position="attributes">
                <attribute name="required">True</attribute>
            </field>
            <!-- doing it by using position="attributes" don't want to do the job.
                 So I used with my muscles with replace.
            -->
            <field name="date_due" position="replace">
                <field name="date_due" attrs="{'readonly': [('state', 'in', ['open', 'in_payment', 'paid'])]}" force_save="1"/>
            </field>
        </field>
    </record>
</odoo>
